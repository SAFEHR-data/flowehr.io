# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: "🌺 FlowEHR"
title_separator: "|"
locale: "en-GB"
url: "http://flowehr.io"
description: >- # this means to ignore newlines until "baseurl:"
  A secure, cloud-native development & deployment platform for research & innovation in digital healthcare.
logo: "/assets/images/hibiscus_apple_emoji.png"
masthead_title: "FlowEHR"

github_username: "UCLH-Foundry"
repository: "UCLH-Foundry/flowehr.io"

minimal_mistakes_skin: "air"
search: false
atom_feed:
  hide: true

# Build settings
markdown: kramdown
remote_theme: mmistakes/minimal-mistakes

# Outputting
permalink: /:categories/:title/
timezone: Europe/London

include:
  - _pages

author:
  name   : "FlowEHR"
  links:
    - label: "GitHub"
      icon: "fab fa-fw fa-github"
      url: "https://github.com/UCLH-Foundry/FlowEHR"



defaults:
  - scope:
      path: "_pages"
      type: pages
    values:
      layout: single

# Exclude from processing.
# The following items will not be processed
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - .idea/

# Plugins (previously gems:)
plugins:
  - jekyll-sitemap
  - jekyll-gist
  - jekyll-feed
  - jemoji
  - jekyll-include-cache
